apiVersion: v1
kind: Service
metadata:
  name: express-server
  namespace: express-server
  labels:
    app: express-server
spec:
  type: ClusterIP
  selector:
    app: express-server
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
